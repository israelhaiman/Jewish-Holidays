apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: holidays-api-ingress
spec:
  rules:
  - host: holidays.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: holidays-api-service
            port:
              number: 80
